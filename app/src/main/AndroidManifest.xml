<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BroadcastReceiver">

        <activity
            android:name=".WchBasicActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".WchBroadReceiver1"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.wch.broadcastreceiver.STANDARD_BROADCAST" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".WchBroadReceiver2"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.wch.broadcastreceiver.STANDARD_BROADCAST" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".WchBroadReceiver3"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="100">
                <action android:name="com.wch.broadcastreceiver.ORDERED_BROADCAST" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".WchBroadReceiver4"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.wch.broadcastreceiver.ORDERED_BROADCAST" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
